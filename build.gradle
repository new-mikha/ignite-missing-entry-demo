plugins {
    id 'java'
    id 'application'
}

java {
  toolchain {
    languageVersion = JavaLanguageVersion.of(8)
  }
}

group = 'org.example'
version = '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

application {
    mainClass = 'org.example.Main'
}

dependencies {
    implementation group: 'org.apache.ignite', name: 'ignite-core', version: "${igniteVersion}"
    implementation group: 'org.apache.ignite', name: 'ignite-log4j2', version: "${igniteVersion}"
    implementation group: 'org.apache.ignite', name: 'ignite-indexing', version: "${igniteVersion}"

    implementation group: 'org.apache.logging.log4j', name: 'log4j-core', version: "${log4jVersion}"
    implementation group: 'org.apache.logging.log4j', name: 'log4j-api', version: "${log4jVersion}"

    testImplementation platform('org.junit:junit-bom:5.9.1')
    testImplementation 'org.junit.jupiter:junit-jupiter'
}

tasks.withType(JavaExec).configureEach {
  javaLauncher = javaToolchains.launcherFor {
    languageVersion = JavaLanguageVersion.of(8)
  }
}

test {
    useJUnitPlatform()
}
